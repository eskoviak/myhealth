description: |
  The HealthFact model -- represents a known health fact
required:
  - type
  - observationDate
  - source
  - data
properties:
  type:
    description: |
      The type of HealthFact
    enum:
      - 'Weight'
      - 'Height'
      - 'BodyFat'
      - 'BloodPressue' 
  observationDate:
    description: |
      ISO 8601 Forat
    type: string
    format: date
  source:
    type:
      - string
  data:
    description: |
      The data represented by the HealthFact.
    type:  
      - object
    patternProperties:
      - $ref: ./definitions.yaml#/definitions.ScalarValue
      - $ref: ./definitions.yaml#/definitions.CompoundValue
